Gu visual: executive/premium SaaS (navy/gold), circular profile pic, horizontal rail tabs. Contact: 0901 303 203 | tamtruong.pk@gmail.com. Work style: constant status updates, Telegram media confirmation, live QA evidence for Kanban (PASS/FAIL). Tools: Playwright w/ persistent profile C:/Users/thanhtam/AppData/Local/hermes/chrome-hermie (no CDP). Multiple gateways are OK.
§
Runtime/UI QA: verify Tailscale in browser DOM+console, not curl/200. Clone = preserve source behavior/data/actions first; redesign after parity. Cleanup = check callers before deleting shared files.
§
Settings child tabs: horizontal rail kéo ngang — BẮT BUỘC cả mobile và desktop. Không dùng dạng dọc/grid/sidebar cho desktop. Dùng scroll ngang có overflow để fit nhiều tabs.
§
Kanban-first: code/UI/content qua Kanban; diagnose-only ≠ fix; “xử lý luôn” ≠ Hannie patch unless explicit bypass. Projects need E2E docs + graph approval. Scope change = block/brief/unblock. Codex: Kevin helper only; worktree + workspace-write; no bypass; Kevin verifies.
§
Anh Tâm requires VERIFICATION after every batch action. API success ≠ real change — must search again to confirm count dropped. Frustrated when 1,350 email deletions claimed earlier were unverified (inbox still full). Show raw evidence (counts, search results) before any plan.